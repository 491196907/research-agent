# 更新记录（Changelog）

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 的组织方式，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 新增

- **多人口令**：`ACCESS_PASSWORDS = "口令=名字, 口令2=名字2"` —— 每人一个口令，进来就认出"你是谁"（旧的单个 `ACCESS_PASSWORD` 仍然可用）
- **使用记录**：每次研究自动记一条（时间 / 访问者 / 主题 / 步数 / 状态 / 报告名），存进 SQLite 的 `runs` 表
- **「📊 使用记录」页**：总次数、参与人数、按访问者统计、明细表格，并可**一键导出 CSV**（`utf-8-sig` 编码，Excel 打开中文不乱码）
- **测试**：新增 9 个（使用记录的写入 / 排序 / 统计 / 脏数据容错，口令解析与校验），共 **42 个**

### 修复

- **云端"半新半旧"导致的红屏**：Streamlit Cloud 更新时，若进程里缓存的 `core/config.py` 还是旧版，调用 `config.check_password` 会抛 `AttributeError`，用户被挡在口令页外。现在 `app.py` 会先探测配置版本，读不到新配置就退回旧的单个口令逻辑，并在界面上提示"云端还在跑旧代码，Reboot 一下"
- **侧边栏显示 `代码版本`**：一眼就能看出云端有没有跑到最新提交
- **弃用参数**：`st.dataframe(use_container_width=True)` → `width="stretch"`（该参数 2025-12-31 后移除），`requirements.txt` 相应提到 `streamlit>=1.49`

### 计划中

- **报告结构升级**：每个子问题独立成节，更像正式研究报告
- **向量持久化**：把 embedding 结果也存进 SQLite（目前只在进程内缓存，重启要重算）
- **资料库管理界面**：在网页上上传 / 删除资料，不用手动放文件
- **成本统计**：累计每次研究的 token 用量，估算花费

## [1.0.0] - 2026-09-17

首个公开版本。

### 新增

- **Agent 循环**：模型自己规划子问题 → 调工具 → 看结果 → 决定下一步，最多 20 步；有最大步数与无进展检测两道保险
- **本地资料检索**：切片 + 向量 + 余弦相似度；配了 embedding key 就是语义检索，没配自动降级为词频检索
- **三种 embedding 后端**：本地 / API（硅基流动 `bge-m3`）/ 假后端（离线测试用），同一接口可切换
- **5 个工具**：`list_sources`（看有什么资料）、`search_notes`（检索）、`save_note`（存要点）、`fetch_web`（抓网页）、`web_search`（联网搜索）
- **联网能力**：优先走 Tavily API；没配 key 时依次回退 Bing / 360，并有通用兜底解析（引擎改版也能抠出链接）
- **数据持久化**：要点存进 SQLite（`outputs/research.db`），重启不丢
- **报告生成**：`outputs/研究报告-<主题>-<时间>.md` —— 结论 + 引用来源清单 + 研究计划 + 每一步运行轨迹
- **Streamlit 网页界面**：实时显示每一步、历史报告浏览与下载、已存要点表格、配置自检面板、可选访问口令
- **日志**：控制台 + 文件双出口，`DEBUG` 一键切换控制台详略（文件始终记全量）
- **测试与 CI**：33 个单元测试（不联网、不花钱）+ GitHub Actions，每次 push 自动在两个 Python 版本上跑
- **开源配套**：MIT 协议、README（含实机截图）、`.streamlit/secrets.toml.example` 配置模板

### 修复

- **API 400（`must be followed by tool messages`）**：`assistant(tool_calls)` 之后必须紧跟对应的 `tool` 消息。现在每次发请求前会自动体检消息序列并修复非法夹层
- **连续检索失败就直接放弃**：改为"先往对话里插一条提示，让模型换策略（联网 / 换词）"；提醒后仍失败，则**强制收尾并要求给出结论**，而不是只回一句"提前结束"
- **报告来源无法核对**：抓取网页的片段来源现在附带**原始网址**（原先只有本地文件名）
- **检索行为不稳定**：`temperature` 默认降到 0.3，同一个主题两次运行的结果更一致
- **失败原因不透明**：搜索失败会明确列出试过哪些引擎、各自什么原因，并给出下一步建议
- **工具报错后看不到堆栈**：`call_tool` 改用 `log.exception`，完整调用栈进日志

### 说明

- 版本号从 `1.0.0` 起算：此前的开发过程没有公开发布，故不单独记版本
